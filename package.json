{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx live-server --entry-file=index.html --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
